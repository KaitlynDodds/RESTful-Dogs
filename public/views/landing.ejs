<% include partials/header %>

<h3>Look at all the doggos</h3>

<a class="ui button" href="/dogs/new" >New Dog</a>

<div class="ui three column padded doubling stackable grid">
        <% dogs.forEach(function(dog) { %>
            <div class="column">
                <div class="ui raised segment dogGallery">
                    <% include partials/ribbon-label %>
                    
                    <h2 class="ui header">
                        <a href="/dogs/<%= dog._id %>">
                            <%= dog.name %>
                            <div class="sub header">
                                <%= dog.breed %>
                            </div>
                        </a>
                    </h2>

                    <a href="/dogs/<%= dog._id %>">
                        <img class="ui centered rounded image" src="<%= dog.image %>"/>                          
                    </a>

                    <div class="description">
                        <%= dog.description %>
                    </div>

                </div>
            </div>
        <% }); %>
</div>

<% include partials/footer %>
